<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sboard.mapper.CommentMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="cno" keyColumn="CNO">
        INSERT INTO TB_COMMENT
            (ANO, CONTENT, WRITER, REG_IP, WDATE)
        VALUES
            (#{ano}, #{content}, #{writer}, #{reg_ip}, SYSDATE)
    </insert>

    <select id="select" resultType="kr.co.sboard.dto.CommentDT0">
        SELECT C.*, U.NICK FROM TB_COMMENT C
        JOIN TB_USER U
        ON C.WRITER = U.USID
        WHERE CNO=#{cno}
    </select>

    <select id="selectAll" resultType="kr.co.sboard.dto.CommentDT0">
        SELECT C.*, U.NICK FROM TB_COMMENT C
        JOIN TB_USER U
        ON C.WRITER = U.USID
        WHERE ANO=#{ano}
    </select>

    <update id="update">
        UPDATE TB_COMMENT SET
            CONTENT=#{content}
        WHERE
            CNO=#{cno}
    </update>
    <delete id="delete">
        DELETE FROM TB_COMMENT WHERE CNO=#{cno}
    </delete>

</mapper>